---
date: 2026-05-12
role: engineer
vendor: Claude Code (Anthropic)
status: 結案
violations: [F2-20260512-04]
related: [2026-05-12_session_s41e_s41f.md]
---

# Reflection: 2026-05-12 Session — S41-G (Search 5/6 trend filter 全量化 + hour 格式修正)

> 對應 F2-20260512-04 個體層雙寫紀錄 (依 `individual-learning-loop §2.3`)。
> 本 sprint 延續同一 session、與 S41-E / S41-F 學習點請見 `2026-05-12_session_s41e_s41f.md` (L26 / L27 在那邊寫)。

---

## 本 sprint 關鍵學習點

### S41-G 本體
- 兩條 bug 相互掩蓋:trend filter 偏差 (數值低) + hour 格式 01-24 vs 00-23 (整段 0) — 後者把前者藏起來
- 「主指標 vs 趨勢視覺化」業務語義切割:KPI 排除取消、趨勢全量 (流量視覺化)
- 修法極簡:builder 移 filter wrapper、parser 改讀 bucket、Padder 0-23 替代 1-24、Suite 加正向錨點

### 紀律啟示
- Suite assertion 必含**正向錨點** (具體 key→value 對應)、不只驗 length 或 first/last 鍵名
- 寫 Padder / Parser 必對照 **ES 實際回傳格式**（如 `Format("HH")` = 00-23）+ **Golden 樣張**、不靠人類直覺
- 兩 bug 互掩的除錯陷阱:先發現哪條決定除錯路徑、修錯一條仍看不到結果、需「全鏈條」對照 Golden

---

## 詳細學習點 (L26 / L27) 寫在主 reflection
請見 `2026-05-12_session_s41e_s41f.md`:
- **L26**:主指標 vs 趨勢視覺化 語義切割
- **L27**:兩條 bug 相互掩蓋的除錯陷阱

---

## S41-G 完工檔案清單
- `PIC.CPF.OrderSDK.Biz.Read.Elastic/DAL/OrderSearchDal.AggregateHelpers.cs`:
  - `AppSalesMetricsTrend` builder 移除 `SMT_Valid` filter wrapper、Sum 直接掛 hist bucket
  - `ParseSalesMetricsTrend` 改讀 `bucket.DocCount` (raw count) 與 `bucket.Aggregations[SMT_SalesAmt]`
- `PIC.CPF.OrderSDK.Biz.Read.Elastic/Extension/ConverToExtension.cs`:
  - `PadTrendDataHourly` 迴圈改 `for (int i = 0; i <= 23)` 對齊 ES `Format("HH")` + Golden
- `CPF.Sandbox/Scenarios/P2_SearchScenarioSuite.cs`:
  - S27 加 `SalesTrend[20]=13499` / `OrderTrend[20]=24` / `first="00"` / `last="23"` 顯式錨點
  - S28 加 `SalesTrend[05/05]=13499` / `OrderTrend[05/05]=24` 顯式錨點

## Verify
- Build: 0 errors
- validate suite: Search 1-7 全 37 項 PASS
- Search 5/6 trend value + hour 格式 + 區間 全對齊 Golden Recipe
