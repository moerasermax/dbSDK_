# Sprint S10 完成報告：CPF 業務模擬沙盒

## 完成項目
- [x] **沙盒環境建立**：實作 `CpfMockSandbox.cs`，整合隨機資料產生器與 Mock Repository。
- [x] **全流程驗證**：支援 AddOrder (新增)、ChangePayType (變更支付)、UpdateSellerMemo (更新備註) 三大業務場景。
- [x] **點符號路徑實作**：成功產出 `c_order_m.xxx` 格式指令，支援子文件局部更新。
- [x] **Null 過濾機制**：確保 `$set` 指令中不包含 `null` 值，防止資料損毀。
- [x] **BsonElement 映射**：驗證 snake_case 轉換邏輯正確。

## 檢核點驗證結果

| 編號 | 檢核項目 | 狀態 | 說明 |
|------|----------|------|------|
| 1 | 隨機資料流入 | ✅ | `AddOrderArgs` 隨機資料成功轉換為 Bson 指令 |
| 2 | 局部更新驗證 | ✅ | `$set` 指令僅包含變更欄位，且使用點符號路徑 |
| 3 | 指令安全性 | ✅ | 自動移除 `null` 欄位，符合開發規範 |

## 技術亮點
- **離線開發能力**：現在開發者可以在不啟動 MongoDB 的情況下，驗證複雜的業務邏輯映射是否正確。
- **自動化驗證**：沙盒內建 PASS/FAIL 檢核，能快速識別欄位缺失或格式錯誤。

## 完成日期
2026-04-24
